import { InfoContainer } from "../InfoStyle";
import UserInfo from "./UserInfo";

const User = () => {
  return (
    <InfoContainer>
      <UserInfo />
    </InfoContainer>
  );
};

export default User;
