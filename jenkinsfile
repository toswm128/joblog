pipeline {
    agent any
    
    stages() {
        
        stage('build') {
            steps {

                dir('front'){
                    sh "docker build -t minsu10/joblog-client ."
                }
                    sh "docker push minsu10/joblog-client"
            }
        }        
    }
}