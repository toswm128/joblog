pipeline {
    agent any
    
    stages() {
        
        stage('build') {
            steps {
                sh "docker images"
                sh "cd front"
                sh "docker build -t minsu10/joblog-client --push ."
            }
        }        
    }
}